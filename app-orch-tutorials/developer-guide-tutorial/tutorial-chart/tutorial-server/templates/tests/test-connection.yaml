apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tutorial-server.fullname" . }}-test-connection"
  labels:
    {{- include "tutorial-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tutorial-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
